# 자가 검증 보고서 — 당직 일정표

> STEP 4 Gap Detector · `prd.md` · `design.md` 와 실제 코드를 항목별 1:1 대조
> 검증 환경: PostgreSQL 16.13 (앱 계정 = DDL 권한 없음) · uvicorn 실행 · 실제 HTTP 호출 · Playwright(Chromium)
> 검증일: 2026-09-08

---

## 1. 종합 판정

| 구분 | 건수 |
|---|---|
| Pass | 41 |
| Partial → 수정 완료 | 5 |
| **Fail → 수정 완료** | **2** |
| 이후 단계로 이월 (STEP 6 패키징) | 3 |
| **미해결** | **0** |

발견된 격차 7건은 **전부 이 단계에서 수정하고 재검증까지 마쳤다.**
회귀 검증 결과: API 18항목 · DB 계층 22항목 **전부 Pass, 미처리 예외 0건.**

---

## 2. 발견하고 고친 격차

### F-1 (Fail · 보안) 엑셀 파서가 시트를 통째로 메모리에 올림

- **문서** — `design.md`: "openpyxl `read_only` 모드"
- **실제** — `load_workbook(..., read_only=False)`
- **왜 문제인가** — 10MB xlsx 는 압축 파일이다. 압축 해제 시 수 GB 로 부풀 수 있고(zip bomb),
  `read_only=False` 는 시트 전체를 메모리에 적재하므로 파드가 OOMKilled 된다.
  업로드 크기 상한 10MB 만으로는 막히지 않는다.
- **조치** — `read_only=True` 로 바꾸고 행을 흘려 읽도록 파서를 재작성.
  머리글 탐지를 위해 필요한 임의 접근은 **행 2000 · 열 60 상한의 격자**로 옮겨 해결했다.
  이로써 읽어들이는 양이 파일 크기와 무관하게 상한에서 끊긴다.
- **재검증** — 원본 엑셀 122건 정상 반영, 잘못된 조직 값 엑셀은 행 번호와 함께 400 거부.

### F-2 (Fail) DB 중단 시 조회·수정 API 가 503 이 아닌 500

- **문서** — `prd.md` 예외 처리 정책: "DB 연결 실패 → 503"
- **실제** — `/api/health` 만 503, `/api/schedule` 등은 **500**
- **원인** — `asyncpg.PostgresError` 만 처리하고 있었다. 이 예외는 **서버가 보고한 오류**만 해당한다.
  DB 프로세스가 죽으면 `InterfaceError`(커넥션 소실)나 `OSError`(Connection refused)가 나는데
  둘 다 잡히지 않아 일반 예외 핸들러로 흘러 500 이 됐다.
- **조치** — `PostgresError` · `InterfaceError` · `OSError` · `TimeoutError` 를 모두 503 으로 처리.
  더불어 **제약 위반(`IntegrityConstraintViolationError`)은 400 으로 분리**했다 —
  재시도해도 똑같이 실패하는 오류를 "일시적 오류"라 안내하면 사용자가 헛되이 재시도한다.
- **재검증** — DB 정지 상태에서 `/api/health` · `/api/schedule` · `/api/history` · `PUT` **전부 503**,
  응답 본문에 접속정보·스택 유출 0건, DB 복구 시 **앱 재기동 없이 자동 정상화**.

### P-1 (Partial) 설계 불변식 "SQL 은 repository.py 에만" 위반

- `db.py` 의 헬스체크가 `SELECT 1` 을 직접 실행하고 있었다.
- 한 줄짜리라 무해하지만, **"SQL 은 한 파일에만 있다"는 전제가 깨지면 인젝션 점검 범위를
  grep 한 번으로 확인할 수 없게 된다.** 규칙의 가치는 예외가 없을 때만 유지된다.
- `repository.ping()` 으로 옮겨 불변식을 실제로 성립시켰다. grep 검증 결과 위반 0건.

### P-2 (Partial) 미사용 import

- `main.py` 가 `repository` 를 import 하고 쓰지 않았다. AST 로 전 모듈을 훑어 확인·제거.

### P-3 (Partial) 반응형 분기점 문서-코드 불일치

- 문서 `940px` vs 실제 CSS `760px`(+`390px` 보조 분기점).
- 실제로 검증된 값은 코드 쪽이므로 **문서를 코드에 맞췄다.**

### P-4 (Partial) F4 기능 설명이 옛 규칙을 서술

- PRD 가 "영기 담당자"라 적고 있었으나, 담당자는 조직과 무관하게 입력 가능하도록 이미 바뀐 상태였다
  (실데이터에 `도강 · 최진필` 행이 존재). 문서를 현재 규칙으로 갱신.

### P-5 (Partial) 예외 처리 정책 표에 미검증 항목 2건

- "DB 연결 실패", "동시 수정 충돌"이 표에만 있고 실제로 확인된 적이 없었다.
- 둘 다 실제로 재현해 검증했다(F-2 는 그 과정에서 발견됐다).

---

## 3. 항목별 대조표

### 3-1. 기능 명세 (PRD 3장)

| # | 기능 | 판정 | 근거 |
|---|---|---|---|
| F1 | 월간 달력 조회 | Pass | Playwright: 35칸 렌더, 월 이동, 오늘 강조 확인 |
| F2 | 모바일 목록 뷰 | Pass | 390px 뷰포트에서 30행 목록 전환 확인 |
| F3 | 다가오는 당직 10건 | Pass | 10건 표시, 날짜순 정렬 확인 |
| F4 | 하루 단위 수정 | Pass | 저장 → 새로고침 후 값 유지 확인 |
| F5 | 하루 일정 비우기 | Pass | 204, 재삭제도 204(멱등) |
| F6 | 변경 이력 | Pass | 10건 조회, 요약문 `2026-09-19 도강 최진필 → 도강 플레이라이트` |
| F7 | 엑셀 일괄 반영 | Pass | 원본 파일 122건 교체, 스냅샷 발급 |
| F8 | 되돌리기 | Pass | 되돌린 뒤 원래 값 복원 확인 |
| F9 | 관리자 키 잠금 | Pass | 키 미주입 시 통과 / 주입 시 401·200 분기 확인 |

### 3-2. 예외 처리 정책 (PRD 6장)

| 상황 | 기대 | 실측 | 판정 |
|---|---|---|---|
| 날짜 형식 오류 | 400 | 400 | Pass |
| 허용되지 않은 조직명 | 400 | 400 | Pass |
| 이름·비고 길이 초과 | 400 | 400 | Pass |
| 정의되지 않은 필드 | 400 | 400 | Pass |
| 조회 범위 초과 | 400 | 400 | Pass |
| 엑셀 형식 불일치 | 400 + 데이터 보존 | 400, 기존 122건 무손상 | Pass |
| 업로드 10MB 초과 | 413 | 413 | Pass |
| 빈 본문 업로드 | 400 | 400 | Pass |
| 없는 날짜 삭제 | 204 | 204 | Pass |
| **DB 연결 실패** | 503 | 500 → **수정 후 503** | **Fail → Pass** |
| DB 제약 위반 | (미정의) | 400 으로 분리 신설 | Pass |
| 동시 수정 충돌 | 행 1개 유지 | 5건 동시 요청 → 행 1개, 이력 5건 | Pass |
| 없는 경로 | 404 problem+json | 404 problem+json | Pass |

### 3-3. 보안 대책 (design 6장)

| 위협 | 판정 | 근거 |
|---|---|---|
| SQL 인젝션 | Pass | `'; DROP TABLE duty_day; --` 저장 시도 → 문자열로만 저장, 테이블 생존. SQL 전량 `$n` 바인딩(상수만 f-string 보간) |
| XSS | Pass | `app.js` 에 `innerHTML` 사용 0건(grep). 전량 `textContent`·`createElement` |
| CSP·클릭재킹·MIME | Pass | 4개 헤더 전 응답에 부착 확인 |
| CSRF | Pass | 쿠키·세션 미사용, 키는 헤더 전용 |
| 대용량 업로드 | Pass | Content-Length 선검사 + 스트림 누적 상한 |
| **엑셀 폭탄** | **Fail → Pass** | F-1 참조 |
| 파괴적 실수 | Pass | 교체 전 전체 스냅샷 → 되돌리기 동작 확인 |
| 정보 노출 | Pass | 오류 응답·로그에 접속정보·키·스택 0건(grep 검증) |
| 비밀정보 하드코딩 | Pass | `os.environ` 접근이 `config.py` 밖에 0건(grep) |
| 최소 권한 | Pass | DDL 권한 없는 앱 계정으로 전 기능 동작, `CREATE TABLE` 은 권한 부족 거부 |

### 3-4. 아키텍처 4대 원칙

| 원칙 | 판정 | 근거 |
|---|---|---|
| 앱/데이터 분리 | Pass | 프로세스 1개가 정적 파일 + `/api/*` 서빙. `/` 마운트가 `/api` 를 가리지 않음을 확인 |
| 무상태성 | Pass | 파일 쓰기 코드 0건. 업로드는 `multipart` 대신 본문 스트림이라 임시파일도 없음 |
| 레거시 격리 | Pass | 사내 DB·API 미연동. 외부 호출 코드 0건 |
| 환경설정 외부화 | Pass | `DATABASE_URL`·`ADMIN_KEY` 등 전량 환경변수 |

### 3-5. 이후 단계로 이월 (STEP 6 패키징에서 검증)

| 항목 | 상태 |
|---|---|
| 비루트 UID 10001 실행 | Dockerfile 작성 시 |
| 8080 포트 리슨 | Dockerfile 작성 시 |
| `readOnlyRootFilesystem` 실제 동작 | 컨테이너 기동 시 |

---

## 4. 검증 재현 방법

```bash
# 1) DB 준비
psql -d duty -f schema.sql

# 2) 데이터 접근 계층 (22항목)
DATABASE_URL=postgresql://... python scripts/verify_db.py

# 3) API (18항목) — 앱을 띄운 뒤
BASE_URL=http://127.0.0.1:8080 ./scripts/verify_api.sh
```

## 5. 남은 한계 (의도된 선택)

- **인증 없음** — 사용자 결정에 따른 현 정책. 접근 통제는 사내망 경계에 의존한다.
  대신 모든 변경을 되돌릴 수 있게 만들어 피해를 제한했고, `ADMIN_KEY` 주입만으로 즉시 잠글 수 있다.
- **변경 주체를 기록하지 않음** — 인증이 없어 신뢰할 수 있는 주체를 특정할 수 없다.
  IP 기록은 목적 대비 과도한 개인정보 수집이라 하지 않는다.
- **레이트 리미팅 없음** — 파드 다중화 시 인메모리 카운터가 부정확해 잘못된 안전감을 준다.
  업로드 상한과 되돌리기로 실제 피해를 제한하는 쪽을 택했다.
