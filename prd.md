# 당직 일정표 — PRD

> kt M&S 사내 AI 도구 플랫폼(kt cloud Managed KS) 배포용
> 작성 기준: 사내 AI 도구 제작 가이드 STEP 1

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | 당직 일정표 |
| 한 줄 설명 | 영업총괄 3팀(영기·소강·도강) 주말·휴일 순환 당직과 영기팀 평일 당직을 한 화면에서 확인하고 수정하는 도구 |
| 핵심 가치 | 엑셀 파일을 열어 두 개의 표를 눈으로 대조하던 일을, 달력 한 장으로 끝낸다 |

**해결하는 문제** — 당직표는 엑셀 시트에 ① 주말·휴일 3팀 순환 표와 ② 영기팀 평일 개별 표로 따로 흩어져 있다.
"이번 주 토요일 누구지?"를 확인하려면 두 표를 오가며 날짜를 맞춰야 하고, 파일을 가진 사람만 볼 수 있으며,
변경이 생기면 수정본을 다시 배포해야 한다. 이 도구는 두 표를 날짜 기준으로 합쳐 달력에 얹고,
링크 하나로 공유하며, 수정은 화면에서 바로 반영한다.

---

## 2. 타겟 유저 및 사용자 시나리오

| 유저 | 규모 | 목적 |
|---|---|---|
| 영기팀 팀원 | 8명 내외 | 내 당직일 확인, 다가오는 당직 확인 |
| 소강·도강팀 팀원 | 소수 | 우리 팀 주말 당직 차례 확인 |
| 당직표 관리 담당자 | 1~2명 | 인원 교대·변경 반영, 연간 엑셀 갱신 |

**시나리오 A — 확인 (가장 잦음)**
출근길에 링크를 열어 이번 달 달력을 본다. 오늘 칸에 표시가 있고, 아래 "다가오는 당직"에서
내 이름이 언제 나오는지 바로 보인다. 로그인 없음.

**시나리오 B — 수정**
"이번 토요일 배병모 → 황성용으로 바꿔야 한다." 수정 모드를 켜고 해당 날짜를 눌러
담당자를 고친 뒤 저장한다. 다른 사람이 새로고침하면 바뀐 값이 보인다.

**시나리오 C — 연간 갱신**
내년 당직표 엑셀을 받았다. 업로드 화면에서 파일을 올리면 전체 일정이 새 표로 교체된다.
잘못 올렸으면 변경 이력에서 직전 상태로 되돌린다.

---

## 3. 핵심 기능 명세

### 필수 (MVP)

| # | 기능 | 설명 |
|---|---|---|
| F1 | 월간 달력 조회 | 날짜별 당직 조직·담당자·비고 표시. 이전/다음 달 이동, 오늘 강조 |
| F2 | 모바일 목록 뷰 | 좁은 화면에서는 달력 격자 대신 날짜별 세로 목록으로 전환 |
| F3 | 다가오는 당직 | 오늘 이후 가장 가까운 10건을 날짜순으로 표시 |
| F4 | 하루 단위 수정 | 구분(평일/휴일)·당직 조직·담당자·비고를 수정하고 즉시 서버 저장 |
| F5 | 하루 일정 비우기 | 해당 날짜의 당직 정보 삭제 |
| F6 | 변경 이력 | 언제 어느 날짜가 무엇에서 무엇으로 바뀌었는지 기록·조회 |

### 선택

| # | 기능 | 설명 |
|---|---|---|
| F7 | 엑셀 일괄 반영 | 당직표 엑셀(.xlsx) 업로드 → 전체 일정 교체. 교체 직전 상태를 스냅샷으로 남김 |
| F8 | 되돌리기 | 변경 이력에서 직전 상태로 복원 |
| F9 | 관리자 키 잠금 | `ADMIN_KEY` 환경변수가 주입된 경우에만 수정 계열 API에 키 요구 |

---

## 4. 기술 스택

| 계층 | 선택 | 버전 | 라이선스 |
|---|---|---|---|
| 런타임 | Python | 3.12 (`python:3.12-slim`) | PSF |
| 웹 프레임워크 | FastAPI | 0.141.1 | MIT |
| ASGI 서버 | uvicorn | 0.52.4 | BSD-3-Clause |
| DB 드라이버 | asyncpg | 0.31.0 | Apache-2.0 |
| 엑셀 파싱 | openpyxl | 3.1.5 | MIT |
| DB | PostgreSQL | 16 | PostgreSQL License |
| 프론트엔드 | Vanilla JS + CSS (빌드 도구 없음) | — | 자체 작성 |

**라이선스 검토 결과** — 위 의존성은 전부 허용 목록(MIT · Apache-2.0 · BSD)에 든다.
DB 드라이버로 흔히 쓰는 `psycopg`/`psycopg2`는 **LGPL-3.0**이라 「검토 필요」 등급이므로
같은 기능을 하는 **Apache-2.0 라이선스의 `asyncpg`로 대체**했다.
프론트엔드는 외부 라이브러리·CDN·웹폰트를 전혀 쓰지 않는다(클러스터 외부 통신 차단 정책 대응).
엑셀 업로드는 `multipart/form-data` 대신 **요청 본문 직접 전송**을 쓴다. Starlette 의 `UploadFile` 은
1MB 를 넘는 업로드를 임시파일로 디스크에 흘려 무상태성·`readOnlyRootFilesystem` 과 충돌하기 때문이다.
덕분에 `python-multipart` 의존성도 필요 없어졌다.

---

## 5. 주요 화면 구성 및 User Flow

**화면은 단일 페이지 하나다.**

```
┌─ 헤더 : 제목 · [수정 모드] · [엑셀 반영] · [변경 이력]
├─ 범례 : 영기 / 소강 / 도강 / 전산휴무 색상
├─ 달력 : 월 이동(← 이번 달 →) + 7열 격자 (모바일은 세로 목록)
└─ 다가오는 당직 : 최근 10건
```

**User Flow**

```
접속 → GET /api/schedule → 달력 렌더
  ├─ 월 이동 → 클라이언트 렌더만 (재요청 없음)
  ├─ 수정 모드 ON → 날짜 클릭 → 모달 → [적용]
  │     → PUT /api/schedule/{date} → 성공 시 화면 갱신 · 실패 시 원복 + 토스트
  ├─ 엑셀 반영 → 파일 선택 → 확인 모달("전체가 교체됩니다")
  │     → POST /api/schedule/import → 스냅샷 저장 후 교체 → 재조회
  └─ 변경 이력 → GET /api/history → 목록 → [되돌리기] → POST /api/history/{id}/revert
```

---

## 6. 상세 기능 명세 및 예외 처리 정책

### 데이터 규칙

- **하루 = 한 행.** 날짜(`duty_date`)가 기본키다.
- `day_type` 은 `평일` · `휴일` 두 값만 허용한다.
- `휴일` 이면 `org` 는 `영기`·`소강`·`도강`·`전산휴무` 중 하나 또는 비움.
- `평일` 이면 `org` 를 비운다(3팀 순환 대상이 아님).
- `person`(담당자)은 **조직과 무관하게** 채울 수 있다. 실제 데이터에 `도강 · 최진필` 처럼
  영기가 아닌 조직 차례에도 담당자가 적힌 행이 있어, 조직으로 제한하면 값이 소리 없이 사라진다.
- 응답은 기존 화면 로직과 호환되도록 `weekendDuty[]` · `weekdayDuty[]` 두 배열로 변환해 내려준다.

### 예외 처리 정책 (RFC 7807 `application/problem+json`)

| 상황 | 처리 | 응답 |
|---|---|---|
| 날짜 형식 오류 (`2026-13-45`) | 서버 검증 거부 | 400 `잘못된 날짜 형식입니다` |
| 허용되지 않은 조직명 | 서버 검증 거부 | 400 `허용되지 않은 당직 조직입니다` |
| 이름·비고 길이 초과(각 50 / 200자) | 서버 검증 거부 | 400 |
| 조회 범위가 과도(1년 초과) | 범위 축소 | 400 |
| 엑셀 형식 불일치 (시트·열 위치 다름) | 파싱 중단, **기존 데이터 보존** | 400 `당직표 형식을 인식할 수 없습니다` |
| 업로드 10MB 초과 | 본문 읽기 전 거부 | 413 |
| DB 연결 실패·중단 | 조회·수정 전 경로에서 503. 상세 원인은 로그에만. DB 복구 시 재기동 없이 자동 정상화 | 503 `일시적으로 처리할 수 없습니다` |
| 없는 날짜 삭제 | 멱등 처리 | 204 |
| 동시 수정 충돌 | 날짜 PK 로 행이 하나로 합쳐짐(마지막 저장 우선). 모든 변경이 이력에 남음 | 200 |

| DB 제약 위반(검증 통과 후) | 재시도해도 실패하므로 503 이 아닌 400 으로 구분 | 400 `저장할 수 없는 값이 있습니다` |

- 모든 오류 응답에 **DB 접속 문자열·스택 트레이스·환경변수 값을 절대 담지 않는다.**
- 로그에도 개인식별정보와 비밀값을 남기지 않는다.

---

## 7. UI/UX 가이드

- **기존 화면의 톤을 유지한다.** 네이비(`#172554`)·블루(`#2563eb`) 기조, 흰 카드 + 옅은 그림자,
  `Pretendard` 우선 시스템 폰트 스택(웹폰트 다운로드 없음).
- 조직 색상 고정: 영기 파랑 `#2563eb` · 소강 초록 `#059669` · 도강 주황 `#d97706` · 전산휴무 회색 `#64748b`.
- **반응형** — 761px 이상 7열 달력, 760px 이하는 날짜별 세로 목록, 390px 이하 여백 축소. 최소 폭 320px 지원.
- 색상만으로 정보를 전달하지 않는다(조직명 텍스트를 항상 함께 표기).
- 저장·실패는 화면 하단 토스트로 알린다. 실패 시 화면 값은 서버 상태로 되돌린다.
- 모든 버튼·모달에 `aria-label`·키보드 조작을 유지한다.

---

## 8. 제약사항 및 보안 요구사항

### 플랫폼 제약 (아키텍처 4대 원칙)

| 원칙 | 이 도구의 준수 방법 |
|---|---|
| 앱/데이터 분리 | 컨테이너 1개. FastAPI가 정적 파일(화면)과 `/api/*` 를 함께 서빙. Dockerfile 1개, `docker-compose.yml` 없음 |
| 무상태성 | 파일·세션을 컨테이너에 쓰지 않음. 일정·이력 전부 PostgreSQL. 업로드 엑셀은 메모리에서 파싱 후 폐기 |
| 레거시 격리 | 사내 운영 DB·API 미연동. 데이터 출처는 팀 당직표 엑셀뿐 |
| 환경설정 외부화 | `DATABASE_URL` 등 전부 환경변수. 소스에 접속정보·키 하드코딩 없음 |

### 보안 요구사항

- **입력 검증** — 날짜·조직·이름·비고·업로드 파일을 전부 서버단에서 Pydantic으로 검증한다. 클라이언트 검증은 편의일 뿐 신뢰하지 않는다.
- **SQL 인젝션** — 모든 쿼리를 asyncpg 파라미터 바인딩(`$1`)으로 작성한다. 문자열 결합 금지.
- **XSS** — 화면 렌더는 `textContent` 로만 값을 넣는다. 사용자 입력을 `innerHTML` 에 넣지 않는다.
- **파괴적 작업 방어** — 엑셀 일괄 교체는 실행 직전 전체 상태를 이력에 스냅샷으로 남겨 되돌릴 수 있게 한다.
- **선택적 잠금** — `ADMIN_KEY` 가 주입되면 수정 계열 API가 해당 키를 요구한다. 미주입 시 사내망 사용자 누구나 수정 가능(현 정책).
- **비밀정보** — 키·접속정보는 환경변수로만 받고, 응답·로그·배포신청서에 실제 값을 쓰지 않는다.
- **컨테이너** — 비루트 UID 10001 실행, 8080 포트 리슨, `readOnlyRootFilesystem` 가능.
- **외부 통신 없음** — 외부 API·CDN·웹폰트를 호출하지 않는다.

### 제약사항

- 인증·권한 체계가 없다(현 정책). 접근 통제는 사내망 경계에 의존한다.
- 개인정보는 **성명만** 다룬다. 사번·연락처·소속코드 등은 저장하지 않는다.
- 예상 데이터량은 연 366행 수준으로 매우 작다. DB 용량은 최소 단위로 신청한다.

---

## 미결 사항

없음. STEP 2 설계로 진행 가능.
