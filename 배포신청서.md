# 배포 신청서 — 당직 일정표

> kt M&S IT혁신팀 AI 도구 플랫폼 (클러스터 `ai-platform`)
> 최초 배포 신청 · 2026-09-08

---

## 배포 신청 내역

| 항목 | 내용 |
|---|---|
| 도구 이름 | **당직 일정표** |
| 한 줄 설명 | 영업총괄 3팀(영기·소강·도강) 주말·휴일 순환 당직과 영기팀 평일 당직을 달력 한 장으로 확인·수정하는 도구 |
| 분류(태그) | **업무** |
| 담당자(팀·성함) | *← 팀명과 성함을 적어 주세요* |
| 이미지 주소와 태그 | `registry.cloud.kt.com/lkexuc2d/duty-roster:1.0` |
| digest | `sha256:0efd126aeecf1a60b0f1653058426e491ed233a13f858d0f7fe7e97eb04f60d5` |
| 컨테이너 개수 | **1** (Dockerfile 1개, 화면과 API를 한 프로세스가 서빙) |

---

## 운영자 매니페스트 작성용

| 항목 | 값 | 근거 |
|---|---|---|
| 리슨 포트 | **8080** | `Dockerfile EXPOSE 8080` · `APP_PORT` 기본값 |
| 실행 사용자(UID) | **10001** | `Dockerfile USER 10001`. 컨테이너 실행으로 `os.getuid() == 10001` 확인 |
| 아키텍처 | **amd64** | Windows x86_64 에서 `docker build` (크로스 빌드 아님) |
| 메모리 요구 | **requests 128Mi / limits 256Mi** | 최대 부하(엑셀 전체 교체) 실측 RSS **55MB**. 업로드 버퍼 10MB 를 더해도 여유 |
| CPU 요구 | requests 100m / limits 500m | 이용자 10명 내외, 상시 부하 없음 |
| **readOnlyRootFilesystem** | **가능** | 전 기능(엑셀 업로드 포함) 호출 후 새로 생긴 파일 **0건** 실측. `PYTHONDONTWRITEBYTECODE=1` + 빌드 시 `compileall` 로 실행 중 쓰기 없음 |
| 업로드 파일 최대 크기 | **10MB** | `MAX_UPLOAD_BYTES` 기본값. 본문을 스트림으로 읽으며 상한 초과 시 즉시 413 |
| **외부 통신** | **없음** | 코드 전수 검사: 외부 HTTP 호출 0건, CDN·웹폰트 참조 0건, 프론트 `fetch` 대상은 전부 동일 출처(`/api/*`) |
| 헬스체크 경로 | **`GET /api/health`** | DB 연결까지 확인. 정상 200 `{"status":"ok","db":"ok"}` / 이상 503 |

### 환경변수 (소스 전수 조사 결과 · 7개 전부)

| 이름 | 필수 | 기본값 | 비밀값 | 설명 |
|---|---|---|---|---|
| `DATABASE_URL` | **필수** | 없음 | **● 예** | PostgreSQL 접속 정보. 미주입 시 앱이 기동을 중단합니다 |
| `ADMIN_KEY` | 선택 | (빈값) | **● 예** | **주입하면** 수정 계열 API가 이 키를 요구합니다. 비워 두면 사내망 사용자 누구나 수정 가능(현 정책) |
| `APP_PORT` | 선택 | `8080` | 아니오 | 리슨 포트 |
| `DB_POOL_MIN` | 선택 | `1` | 아니오 | 커넥션 풀 최소 |
| `DB_POOL_MAX` | 선택 | `5` | 아니오 | 커넥션 풀 최대 |
| `MAX_UPLOAD_BYTES` | 선택 | `10485760` | 아니오 | 업로드 상한(바이트) |
| `LOG_LEVEL` | 선택 | `INFO` | 아니오 | `DEBUG`·`INFO`·`WARNING`·`ERROR` |

> **비밀값 2개(`DATABASE_URL`·`ADMIN_KEY`)는 Secret 으로 주입 부탁드립니다.**
> 실제 값은 이 문서에 적지 않았습니다. 앱은 이 값들을 로그·응답·오류 메시지 어디에도 출력하지 않으며,
> 기동 로그에는 `database_url: '설정됨'` 처럼 **설정 여부만** 남깁니다.

---

## DB 요청

| 항목 | 내용 |
|---|---|
| 종류·버전 | **PostgreSQL 16** |
| 스키마 | **첨부 `schema.sql` 최초 1회 실행 요청** |
| 저장 항목 | `duty_day` — 날짜별 당직 1행(구분·조직·담당자·비고)<br>`duty_change_log` — 변경 이력과 되돌리기용 스냅샷 |
| 예상 용량 | **10GB** (플랫폼 최소 단위로 신청. 실제 필요량은 연 366행 수준으로 수 MB) |
| 앱 계정 권한 | **`SELECT` / `INSERT` / `UPDATE` / `DELETE`** — **DDL 불필요** |

> **앱은 기동 시 테이블을 만들지 않습니다.** 파드가 여러 개로 뜰 때 서로 충돌하기 때문에 DDL 을 분리했습니다.
> DDL 권한 없는 계정으로 전 기능이 동작하는 것을 실제로 검증했습니다 —
> 그 계정에서 `CREATE TABLE` 은 권한 부족으로 거부됩니다.
>
> `schema.sql` 은 **여러 번 실행해도 안전**합니다 (`CREATE TABLE IF NOT EXISTS` · `ON CONFLICT DO NOTHING`).
> 초기 데이터로 2026년 9~12월 당직 122건이 함께 들어갑니다.

---

## 1차 자가 점검 결과

**자가 검증(STEP 4)** — 상세는 첨부 `자가검증보고서.md`

| 구분 | 건수 |
|---|---|
| Pass | 41 |
| Fail → 수정 완료 | 2 |
| Partial → 수정 완료 | 5 |
| **미해결** | **0** |

수정한 주요 결함 2건:
1. 엑셀 파서가 시트를 통째로 메모리에 적재 → 압축 폭탄으로 OOM 가능. `read_only` + 행·열 상한으로 재작성
2. DB 중단 시 500 반환 → `InterfaceError`·`OSError` 미포착이 원인. 4종을 503으로, 제약 위반은 400으로 분리

**QA(STEP 5)** — 코너 케이스 5종 도출 후 방어 로직 추가
- 작년/타팀 엑셀 오업로드 → **업로드 전 미리보기 필수화** + 기간 미겹침·건수 급감 경고
- 동시 편집으로 남의 변경 덮어쓰기 → 저장 직전 재확인
- 변경 없는 저장으로 이력 오염 → 요청 자체를 보내지 않음
- 휴일→평일 전환 시 비고 소실 → 사라질 값을 명시하고 확인
- 데이터 범위 밖 조회 시 빈 화면 → 현재 등록 범위 안내

**보안 점검 요약**
- SQL 은 단일 파일(`repository.py`)에만 존재하며 전 구간 파라미터 바인딩. 인젝션 문자열 저장 시도 → 문자열로만 저장되고 테이블 생존 확인
- 화면 렌더는 `textContent` 전용, `innerHTML` 사용 0건
- 응답 헤더에 CSP(`default-src 'self'`)·`nosniff`·`X-Frame-Options: DENY`·`Referrer-Policy` 부착
- 오류 응답은 RFC 7807 형식이며 스택·SQL·접속정보 미포함
- 검증 재현: `scripts/verify_db.py`(26항목) · `scripts/verify_api.sh`(18항목) — 전부 통과, 미처리 예외 0건

> 사내 소스코드 진단툴 **스패로우(Sparrow)** 사전 자가 진단은 수행하지 않았습니다.
> 접속 정보를 받을 수 있으면 배포 전에 돌려보겠습니다.

---

## 함께 보내는 파일

| 파일 | 용도 |
|---|---|
| 본문 (이 문서) | 메일 본문에 붙여넣기 |
| **`schema.sql`** | **첨부.** 운영자가 최초 1회 실행 |
| `자가검증보고서.md` | 첨부(선택). 1차 점검 상세 |
| 썸네일 이미지 | 별도 첨부 — 1280×720(16:9) · 500KB 이하 · **실제 사내 데이터가 보이지 않는 화면** |
